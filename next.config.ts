import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Turbopack is the default dev bundler in Next.js 15
  // No additional config needed to enable it
};

export default nextConfig;
